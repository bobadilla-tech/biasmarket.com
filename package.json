{
  "name": "biasmarket",
  "version": "1.0.0",
  "description": "A store builder for K-pop GOMs, photocard sellers, and fan merch shops that need clean orders, payment proofs, and buyer communication without spreadsheet chaos.",
  "author": "Bobadilla Technologie",
  "packageManager": "pnpm@10.11.0",
  "type": "module",
  "scripts": {
    "test": "DATABASE_URL=\"${DATABASE_URL:-postgresql://build:build@localhost:5432/build?schema=public}\" turbo run test",
    "dev": "turbo run dev --parallel",
    "build": "DATABASE_URL=\"${DATABASE_URL:-postgresql://build:build@localhost:5432/build?schema=public}\" turbo run build",
    "lint": "turbo run lint",
    "fix": "node scripts/lint-changed-files.mjs --write && pnpm lint",
    "typecheck": "DATABASE_URL=\"${DATABASE_URL:-postgresql://build:build@localhost:5432/build?schema=public}\" turbo run typecheck",
    "db:generate": "turbo run db:generate",
    "kuma:setup": "node scripts/setup-kuma.ts",
    "docker:dev": "docker compose -f infra/docker/docker-compose.dev.yml up --build",
    "docker:dev:down": "docker compose -f infra/docker/docker-compose.dev.yml down",
    "admin:create:dev": "docker compose -f infra/docker/docker-compose.dev.yml exec api pnpm --filter api run admin:create",
    "admin:promote:dev": "docker compose -f infra/docker/docker-compose.dev.yml exec api pnpm --filter api run admin:promote",
    "seed:base:dev": "docker compose -f infra/docker/docker-compose.dev.yml exec api pnpm --filter api run seed:base",
    "seed:append:dev": "docker compose -f infra/docker/docker-compose.dev.yml exec api pnpm --filter api run seed:append"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "concurrently": "^10.0.5",
    "nodemon": "^3.1.10",
    "prettier": "^3.4.2",
    "socket.io-client": "^4.8.3",
    "turbo": "^2.10.12",
    "typescript": "^7.0.2"
  }
}
