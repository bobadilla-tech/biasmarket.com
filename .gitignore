node_modules
.env
.env.local
dist
.next
.turbo
*.log
postgres_data/
.pnpm-store/
.claude/
coverage/
cookies.txt
.mailer-dev/

# infra/vps/ blue/green prod stack — real secrets and runtime-generated
# state, never committed. .example templates are unaffected (different
# suffix, doesn't match these globs) — see infra/vps/env/*.env.example.
infra/vps/env/*.env
infra/vps/caddy/active/*.caddy
infra/vps/state/
infra/vps/releases/